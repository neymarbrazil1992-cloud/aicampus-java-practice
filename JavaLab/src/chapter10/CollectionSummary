
Collection(자바2-기능) : Generic
 | 
(상속)
 ├── List(부모):가 앞에옴            List<String>student = new ArrayList
 │    ├── ArrayList(!!일반적)
 │    ├── LinkedList ->Linked 배열 알고리즘 : 단순 반복 작업에 빠름
 │    ├── Stack
 │    └── Vector
 │
 └── Set(부모)
      ├── HashSet-중복 점검
      └── SortedSet(정렬)-내림차순 오름차순
           └── TreeSet

Map
 ├── HashMap
 ├── Hashtable
 └── SortedMap
      └── TreeMap

for (int=0; i<list.size();i++) {
     system.out.println(list.get(i));
} ->ArrayList전용

Iterator<Integer> it = list.iterator();
while (it.hasNext()) {
     system.out.println(it.next());
} -> 거의 모든 컬렉션에 범용적으로 쓸 수 있음

for (int=0; i<list.size();i++) {
     if(list.get(i) % 2 == 0) { --->바깔에서 리스트만 건드림 
          list.remove           ---? i 는 리스트가 줄어든 걸 모름 건너뜀 

     }
}
while (it.hasNext()) {
     system.out.println(it.next());
     if (v % 2 == 0){
          it.remove(); -> 커서 자신이 방금 꺼낸 요소를 지움
     }
}

LinkedList 
1|A  2|B   3|C   4|D   5|E  => indexNo.를 갖고 들어간다
-> Data가 그 다음 인덱스를 가지고 있다 